{
  "name": "day-11---markdown-editor",
  "version": "1.0.0",
  "description": "1. **Headings**: Start with `#` for headings. More `#` symbols mean smaller headings.\r 2. **Text Formatting**: Use `**bold**`, `_italic_`, and `**_bold and italic_**`.\r 3. **Lists**: Create unordered lists with `-` or `*`, and indent for nested items.\r 4. **Links and Images**: Use `[text](URL)` for links and `![alt text](URL)` for images.\r 5. **Code Blocks**: Use triple backticks (\\```) to create code blocks, and specify the language for syntax highlighting.\r 6. **Blockquote**: Use `>` to create blockquotes.\r 7. **Tables**: Use pipes `|` to separate columns and dashes `-` to create table headers.",
  "main": "webpack.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
